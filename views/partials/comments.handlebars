{{!retrieved from the single-post}}
{{! we are using "this" to refer to the parent object or array thats available in the cnotext of the partial}}
<h4>
  Below are the Comments:
</h4>
<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>
